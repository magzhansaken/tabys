#!/usr/bin/env bash
# =====================================================================
# ТАБЫС — показать код подтверждения из лога сервера.
# Нужен, пока не подключён СМС-шлюз: запросите код на сайте,
# потом запустите этот скрипт и увидите его.
# Запуск:  bash /opt/tabys/deploy/06_otp_code.sh
# =====================================================================
set -uo pipefail
echo "=== Последние коды подтверждения (свежие сверху):"
docker logs --tail 200 tabys-server 2>&1 | grep "\[OTP\]" | tail -5 | tac
echo
echo "Если пусто — запросите код на сайте и повторите."
echo "Код действует ограниченное время, обычно 10 минут."
