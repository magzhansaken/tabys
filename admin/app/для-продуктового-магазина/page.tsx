'use client';
/**
 * Посадочная страница под тип магазина: продуктовый — ежедневные накладные,
 * весовой товар, долги соседей.
 *
 * Каркас и тексты — в lib/site.tsx (StoreLanding): у каждого типа СВОЙ текст,
 * а не копия главной. Владелец ищет «программа для продуктового магазина» и
 * попадает сразу на свой случай — это и поиск, и доверие.
 */
import { StoreLanding } from '../../lib/site';

export default function Page() {
  return <StoreLanding type="продуктовый" />;
}
