# Пересборка кассы по донору — строгий порядок

Их касса: **9 653 строки**, 43 файла (без проверок).
Разбираю **по 500 строк**, не пропуская середину. **20 частей.**

Правило: часть не обрывается посреди действия — если оно длиннее,
дочитываю до конца и записываю, где остановился.

---

## Часть 1 · main.tsx 1–500 — начало и состояние ✔

**Что у них.** Первая строка объясняет всё устройство:

> «Машина состояний: активация → PIN → смена → заказ ⇄ оплата. Каждое
> действие с деньгами сначала ложится в офлайн-журнал, потом уходит на
> сервер; идентификаторы — ULID, повторная отправка безопасна.»

Ключи хранилища: deviceKey, place, terminalId, location, waiter,
token, user, perms, discountLimit, referral, menuUrl, shiftId.

**Что взято.**

| Их | Наше | Почему |
|---|---|---|
| ULID вместо случайного ключа | **взято** | чеки в журнале лягут по порядку сами |
| «сперва в журнал, потом на сервер» | уже есть | outbox так и работает |
| perms — права кассира | **следующей частью** | сейчас любой вошедший может всё |
| discountLimit — предел скидки | **следующей частью** | сейчас скидка без предела |
| waiter, referral, menuUrl | **не берём** | ресторанное: официанта и меню в магазине нет |
| place, terminalId, location | у нас своё | магазин и касса, не заведение и стол |

**Сделано.** ULID перенесён.

Поймана ловушка, которой у донора нет: касса создаёт ключи подряд
быстрее, чем тикают часы. Метка времени одинаковая, и дальше порядок
решал бы случай — проверил, три ключа вышли не по порядку.

Добавлен счётчик миллисекунды. Проверено на 2000 ключах: все разные и
строго по порядку.

Буквы I, L, O, U выброшены: их путают с 1, 0 и V при диктовке.

---

## Часть 2 · main.tsx 500–1000 — уклад, права, связь ✔

**Что у них.**

*Уклад работы кассы.* «В зале официант ведёт несколько столов и после
заказа хочет видеть список; за стойкой кассир бьёт чек за чеком и
хочет чистый экран. Настройка живёт на устройстве.» — **не берём**,
ресторанное.

*Право сильнее уклада.* Их довод дословно: «У кого нет права брать
деньги — тому и кнопки оплаты видеть незачем, даже если он подошёл к
стойке, где стоит кассовый режим. Иначе официант жмёт „Оплатить",
получает запрос PIN и не понимает, что сделал не так.»

*Связь пропала.* «Уход в офлайн говорится ОДИН РАЗ, а не при каждой
проверке.»

*Escape закрывает верхний диалог.* «ОДИН список на ВСЕ диалоги: раньше
половина окон на Esc не отвечала, и кассир не мог предсказать, что
сделает клавиша.» — **уже есть у меня.**

**Сделано.**

**Связь говорит по одному разу.** У меня было хуже, чем у них: не
говорилось ВОВСЕ. Точка в углу гасла, но её не замечают за работой.

Кассир бьёт чеки час, уверенный, что всё уходит, а они копятся — и
узнаёт об этом при закрытии смены, когда сверка не сойдётся.

Повторять каждые тридцать секунд тоже нельзя: сообщение закроет товар
и кассир перестанет читать сообщения вовсе.

Проверено на десяти проверках подряд: пять неудач — **одно**
сообщение, возврат связи — **одно**.

**Найдено, берётся частью 3.**

**Права кассира не доходят до кассы.** Сервер их ОТДАЁТ — в ответе
входа лежит `permissions`, `isOwner`, `isShiftAdmin`, `canSeeRevenue`.
Касса их не смотрит: любой вошедший может всё.

---

## Часть 3 · main.tsx 1000–1500 — права человека ✔

**Что у них.**

Права берутся у ЧЕЛОВЕКА, а не у устройства:

```
const senior   = policyOf(perms, 'order.item.remove') === 'allowed';
const canOrder = policyOf(perms, 'order.create') !== 'denied';
```

Их довод: «У кого нет права брать деньги — тому и кнопки оплаты видеть
незачем. Иначе он жмёт „Оплатить", получает запрос PIN и не понимает,
что сделал не так.»

*Отправка на кухню, признак `item.fired`* — **не берём**, ресторанное.
Но их урок записан и годится: «признак живёт В ПОЗИЦИИ: отдельный
список умирал вместе с перезагрузкой страницы».

**Сделано.**

**Старший кассир больше не зовёт сам себя.** У меня охрана действий
брала уровень из настройки МАГАЗИНА: «отмены только с разрешения».
Настройка верная, но одна на всех — и старший кассир стоял у кассы и
вводил свой же код, потому что касса не знала, кто перед ней.

Теперь знает: сервер отдаёт `isOwner` и `isShiftAdmin`, они лежат в
пропуске и работают без сети. Кто вправе разрешать — разрешает молча, а
в журнал пишется его имя.

**Оба правила работают вместе, и это верно:**
настройка магазина — про магазин, право человека — про человека.

**Попутно: имени кассира не было в ответе входа.** А касса пишет его в
ЧЕК и в журнал «кто разрешил». В чеке стояло пусто.

---

## Часть 4 · main.tsx 1500–2000 — предел скидки ✔

**Что у них.**

*Страж забытой смены.* Их довод дословно:

> «Автозакрытия в 06:00 нет и не будет: Z — акт гашения с пересчётом
> живых денег, автомат ящик не пересчитает. Но если смена открыта до
> утренней границы — сегодняшние чеки поедут во вчерашнюю, и отчёты
> сольются. Кричим заметно и ведём к закрытию, не блокируя работу.»

*Предел скидки кассира* — через `ElevateDialog`: кассир просит скидку,
старший вводит код, сервер возвращает ваучер. — **берётся частью 5.**

*Часы смены на экране* — `shiftHours` в шапке. — **взято вместе со
стражем.**

**Сделано.**

**Страж забытой смены.** Кассир ушёл домой, не закрыв смену. Утром сел
другой и бьёт чеки — они ложатся во ВЧЕРАШНИЙ день. Владелец смотрит
выручку за вчера и видит чужие деньги, а за сегодня — пусто.

Не запрещаем работать: очередь у кассы важнее отчёта. Но полоса
поверх экрана, мимо неё не пройти.

Проверено пятью случаями:

| Случай | Страж |
|---|---|
| открыли утром, сейчас день | молчит ✔ |
| забыли со вчера | кричит: 25 ч ✔ |
| ночная смена, ещё ночь | молчит ✔ |
| ночная перешла за утро | кричит: 10 ч ✔ |
| длинный день, та же смена | молчит ✔ |

Пятый случай разобран отдельно: ночной магазин работает до утра, и
смену надо закрыть утром — иначе выручка ночи и дня сольются. Страж
прав.

---

## Часть 5б · main.tsx 2000–2500 — что можно трогать в позиции ✔

**Что у них.** Свёртка `canTouchLine`: с позицией, ушедшей на кухню,
нельзя обращаться как с пустой строкой. Отказ **объясняет себя** — не
«нельзя», а «Блюдо уже готовят. Снять может только старший».

**Сделано.**

**Лишняя марка оставалась в чеке.** В магазине считанная марка — то же
сделанное действие, что их бегунок на кухне.

Было 3 бутылки и 3 марки. Убрали одну — осталось **2 бутылки и ТРИ
марки**. Лишняя ушла бы в налоговую как проданная, а товар стоит на
полке. Проверено: теперь 3→2→1, марки сходятся на каждом шаге.

**Отказ при оплате называет товар.** Было «Не все марки
отсканированы». Стало: «Осталось отсканировать марок: 4 — Водка
«Алтай» (2), Сигареты Winston (2)».

---

## Часть 5 · main.tsx 2000–2500 — четыре уровня права ✔

**Что у них.** Четыре уровня вместо моих трёх:

| Уровень | Что значит |
|---|---|
| `allowed` | можно молча |
| `self_pin` | **свой код** — подтверди, что это ты |
| `elevated_pin` | код старшего |
| `denied` | нельзя вовсе |

Их слова: «denied: строка просто не снимается — **касса не спорит, а
не пускает**».

И отдельно: «Снятие позиции подтверждает менеджер — для проверки PIN
нужна связь».

**Сделано.**

**Взят четвёртый уровень — свой код.** Кассир снимает товар из чека
при покупателе, отойти не может — но подтвердить, что руку приложил
именно он, стоит: за отмены спрашивают.

Старшего звать не надо: человек подтверждает СЕБЯ. Сверяется с
пропуском — тем же следом, что при входе без сети, и работает без
интернета.

Проверено на всех четырёх, для кассира и для старшего.

**Взят отказ без связи.** Проверка кода старшего идёт на сервер:
интернета нет — ответа нет. Раньше касса давала вводить код впустую, и
кассир при покупателе трижды набирал и решал, что она сломалась.

Теперь говорим прямо и подсказываем выход: старший может войти своим
кодом — пропуск работает без сети.

**Не взято, и намеренно: ваучер старшего.**

У них сервер выдаёт подписанную бумажку, касса носит её в хранилище и
прикладывает к событию.

**Мой путь надёжнее:** сервер СРАЗУ пишет разрешение в `action_approval`
и `audit_log`, касса ничего не носит.

Их ваучер лежит в хранилище **бессрочно**: строка 1908 кладёт, строки
1560 и 1687 берут, никто не стирает. Разрешил старший одну скидку —
касса приложит ту же бумажку ко всем следующим.

---

## Часть 6 · main.tsx 2500–2961 — замок при простое ✔

**Что у них.** Их довод дословно:

> «Официант отошёл — и любой может пробить чек от его имени. Но
> выходить насовсем нельзя: незаконченный заказ пропадёт, а гость сидит
> за столом. Поэтому касса не выходит, а **ЗАПИРАЕТСЯ**: поверх экрана
> ложится замок, сама касса со всем набранным остаётся живой под ним.
> Ввёл PIN — продолжил с той же строки.»

Три минуты: «меньше — мешает разговаривать с гостем, больше — уже
небезопасно».

**Сделано.**

**Замка при простое у меня не было.** Кассир отошёл в подсобку, к
полке, покурить — касса открыта под его именем. Кто угодно пробьёт
чек, даст скидку, вернёт деньги, и всё уйдёт на него.

Кнопка «Выйти» есть, но нажимать её надо **помнить**, а отходят не
думая. Замок работает сам.

**Касса под замком живая** — чек набран, покупатель ждёт. Ввёл код и
продолжил. Ради этого замок и нужен.

**Отпирается по тому же пропуску,** что и вход без сети — значит
работает без интернета. Подошёл сменщик со своим кодом — открылось, и
**кассир в чеке сменился**: дальше пробивает он.

**Ноль в настройке значит «не запирать»** — на кассе, стоящей в
закрытой комнате, замок только мешает.

---

## Часть 6 · main.tsx 2500–2961 — ждёт разбора

## Части 3–20 — ждут разбора
## Часть 7 · screens/Order.tsx 1–300 — экран продажи ✔

**Что у них.**

*Ходьба вглубь:* «дом показывает группы, тап по группе — её подгруппы,
тап по подгруппе — товары». — **не берём**: у продуктового магазина
категорий десяток, а не сотня. Лишний уровень — лишнее нажатие при
очереди.

*Сквозной поиск:* «Поиск сквозной и уровней не знает: официант помнит
блюдо, а не путь к нему». — **уже есть у меня**, проверено.

*Клавиатура уходит после выбора:* «Блюдо выбрано — клавиатура своё
отработала и уходит сама: она нужна для поиска, поиск завершён, а снизу
ей больше нечего заслонять».

*Касса в двух вкладках* — предупреждение о задвоении номеров.

*Смена идёт 14 часов* — напоминание закрыть.

**Сделано.**

**Поиск и клавиатура уходят после выбора товара.** У меня они
оставались: кассир добавил хлеб, а клавиатура закрывает половину
экрана, и в поиске висит «хле». Следующий товар он ищет поверх старого
слова — и находит не то.

**Вторую копию кассы больше не открыть.** Обе копии считали номера
чеков от своего: пошли бы ДВА чека под номером 57, и налоговая увидела
бы задвоение. Теперь вторая не запускается, а первая поднимается
наверх — кассир ткнул в ярлык дважды и видит свою же кассу.

**Забытая смена — у меня уже вернее, чем у них.** Они говорят «смена
идёт 14 часов», но смена может законно идти шестнадцать: заведение
работает с десяти до двух ночи, и предупреждение сработает зря.

У меня ловится именно **забытая**: открыта до шести утра, а сейчас уже
после — значит вчерашняя. Не трогаю.

---

## Часть 8 · screens/Order.tsx 300–608 — чек и кнопки ✔

**Что у них.**

*Каждая недоступная кнопка объясняет себя подписью:* «Сначала выберите
стол», «Состав изменился после печати — перепечатайте счёт», «Блюдо уже
готовят».

*Возраст заказа* «открыт 12 мин», тикает раз в минуту. — **есть у
меня.**

*Строка связи:* «Связь: онлайн · очередь пуста · последняя отправка
14:20».

**Сделано.**

**Две гашеные кнопки заговорили.** Проверил все недоступные кнопки:
три молчали.

Кассир жмёт цифру при оплате, ничего не происходит, а почему —
неясно. Он жмёт сильнее, зовёт старшего, звонит в поддержку. И всё
из-за одной ненаписанной строки.

- цифры при оплате → «Сначала выберите, куда вносить: наличные или
  карта»
- «Без сдачи» → «Работает только с наличной частью»

Третью — «Скидка» на пустом чеке — **оставил молчать**: чека нет,
скидывать нечего. Объяснять очевидное значит приучить не читать
подписи.

**Время последней отправки.** У меня было «не отправлено: 3» и «без
связи 3 ч», но не было главного: **когда ушло в последний раз**.

Кассир не знал, копится это минуту или третий час. Связь могла
вернуться и снова пропасть, а он думает, что всё стоит с утра.

Стало: «не отправлено: 3 · без связи 3 ч · осталось 69 ч · **ушло в
12:35**».

---

## Часть 9 · screens/Pay.tsx — экран оплаты ✔

**Что у них.** Свёртка `quickTenderOptions`: сперва точная сумма, потом
округления вверх до сотни, пятисот, тысячи, потом купюры, потом десятки
тысяч. Четыре кнопки.

*Наличные подставлены сразу всей суммой:* «точный расчёт — самый частый
случай, а кассир, у которого гость даёт больше, поправит одним
касанием. С нуля кнопка „Принять оплату" была погашена, и человек искал,
что не так, вместо того чтобы закрыть чек». — **есть у меня.**

**Сделано.**

**Быстрые суммы по их правилу.** Мои купюры были, но хуже:

| К оплате | Моё | Их |
|---|---|---|
| 1 340 | 2000 5000 10000 20000 | **1340** 1400 1500 2000 |
| 890 | 1000 2000 5000 10000 | **890** 900 1000 2000 |
| 24 000 | **— НИЧЕГО** | **24000** 30000 40000 50000 |

Три беды в моём:

1. **Нет точной суммы.** «Под расчёт» была отдельной кнопкой, а это
   самый частый случай — должен быть первым.
2. **Нет ближних округлений.** К оплате 1 340 покупатель чаще даёт
   1 400 или 1 500, чем 2 000.
3. **На больших суммах пусто.** 24 000 — ни одной кнопки, купюры
   кончились. Кассир набирал руками при очереди, хотя покупатель дал
   25 000.

Взял их правило целиком, своё убрал. **Пусто не бывает никогда.**

**Купюры живут в одном месте.** Сперва я добавил второй ряд — два ряда
с теми же числами сбивают: кассир не знает, какой жать. Убрал.

---

## Часть 10 · print.ts + PrinterDialog.tsx — печать ✔

**Что у них.**

*Два пути печати:* сперва в принтер байтами, не вышло — через окно
печати. «Работает на любом принтере».

*Виртуальные принтеры:* «PDF, XPS, OneNote, факс не понимают сырые
ESC/POS-байты: очередь принимает задание **успешно**, а файл выходит
пустым».

*Пустое имя принтера:* «сырьё улетело бы в принтер по умолчанию
Windows, а это лотерея — часто PDF».

**Сделано.**

**Перепечатка чека.** Хранилище умело отдавать последние чеки, но
наружу это не было выведено: кассир **не мог перепечатать даже
последний**.

Кончилась лента, отошёл провод — чек сохранён, покупатель уходит без
бумаги, а кассир видит ошибку и не знает, что делать.

Теперь в меню «Повторить печать». Берёт последний напечатанный, а нет
его — последний из хранилища: кассу могли перезапустить, пока меняли
ленту.

**Виртуальный принтер назван в списке.** Владелец ставит кассу и тычет
в первый принтер — а там часто «Microsoft Print to PDF», он есть
всегда.

Касса скажет «чек напечатан», бумаги не будет, и **покупатели уйдут без
чеков весь день**, а причины никто не поймёт.

Теперь в списке: «Microsoft Print to PDF — **НЕ ПЕЧАТАЕТ ЧЕКИ**». И
если такой уже выбран — сказано вслух: подпись в списке видна только
когда список открыт, а беда тихая.

Не запрещаем: бывает, что человек вправду хочет в файл.

---

## Часть 10 · print.ts + printer.cjs — печать ✔

**Что у них.** Три пути печати: агент устройства, настольная версия,
запасной через окно браузера. Настройки: ширина ленты, число копий,
проверка принтера, виртуальный принтер.

**Сверка — почти всё уже есть, и местами лучше.**

| Их | У меня |
|---|---|
| ширина ленты 58/80 мм | **есть**, настраивается |
| денежный ящик | **есть**, открывается при наличных |
| выбор принтера | **есть**, список из Windows |
| проба печати | **есть** |
| очередь заданий | **есть**, и это моё правило |
| число копий | **не было** |

**Моя печать местами надёжнее.** Два моих правила, которых у них нет:

1. **Очередь заданий.** «Чек и копия могут уйти в одну секунду.
   Параллельная отправка перемешивает задания и рвёт ленту посреди
   чека.»
2. **Без общего доступа к принтеру.** Пишу сырые байты прямо в очередь
   печати через `winspool`, а не копирую файл на `\\localhost\Имя` —
   тот способ требует включить «Общий доступ» в Windows, и он слетает
   после обновлений.

**Сделано: число копий чека.**

Их довод: «одна гостю, вторая заведению — на второй кассир ставит
подпись и кладёт в ящик».

В магазине то же: вторая идёт с товаром при доставке, а при продаже в
долг покупатель на ней расписывается.

**Копии печатаются одним заданием.** Отдельные встают в очередь порознь
и могут перемешаться с чужим чеком — лента порвётся посреди. Это моё
же правило про очередь, применённое к копиям.

---

## Часть 11 · Checks.tsx + queue.ts — отклонённые чеки ✔

**Что у них.** Отдельный ящик для событий, которые сервер не принял.
Их довод дословно:

> «События, которые сервер не принял: **деньги не должны пропадать
> молча**. Раньше чек уходил в никуда, а в отчёте пусто, и никто не
> знал почему. Теперь откладываем в отдельный ящик: очередь не
> забивается, но деньги остаются на виду.»

**Сделано.**

**Отклонённый чек крутился в очереди вечно.** Проверил на пяти кругах
отправки:

```
круг 1: ушло 2, осталось 1 — b
круг 2: ушло 0, осталось 1 — b
круг 3: ушло 0, осталось 1 — b   ← и так навсегда
```

Сервер отклонял снова и снова. **Очередь не пустела никогда**: кассир
видел «не отправлено: 3» вечно и переставал этому верить — а значит
пропустил бы настоящую пропажу связи.

А чек с деньгами так и не доходил до отчёта, и **никто об этом не
знал**.

Теперь после правки:

```
круг 1: ушло 2, отложено 1, осталось 0
круг 2: очередь пуста ✔
```

**В меню «Сервер не принял»** — с числом прямо в подписи: «2 чека не
попали в отчёт — покажите владельцу». Кассир не починит сам, но должен
видеть, что показать и какой номер назвать по телефону.

**Очистка убирает только список**, сами чеки лежат на кассе и никуда
не деваются.

---

## Часть 12 · ShiftClose.tsx — закрытие смены ✔

**Что у них.**

*Расхождение:* «Сходится · Излишек · Недостача» с суммой.

*Пустое поле = согласен с расчётом:* `cash === '' ? expectedCash :
closing`.

*Сводка не пришла:* «Сводка недоступна без связи. Смену можно закрыть —
отчёт досчитается на сервере».

*Очередь при закрытии:* «В очереди N неотправленных чеков — они уйдут
вместе с закрытием».

**Сделано.**

**Пустое поле давало НОЛЬ — недостачу на всю кассу.**

```
в ящике по расчёту: 45 000 ₸
поле пустое → записано: 0 ₸
в отчёт уйдёт НЕДОСТАЧА 45 000 ₸
```

Кассир закрывает смену не глядя, и назавтра его спросят о пропавших
деньгах, **которых он не брал**.

У донора пустое значит «согласен с расчётом» — верно, но мягко: тогда
пересчёт можно и не делать вовсе, а ради него всё и затевается.

Взял их правило и **спрашиваю прямо**: «Вы не вписали, сколько
насчитали. „Да" — пересчитал, всё сходится. „Отмена" — вернуться и
вписать: потом доказать будет нечем».

**У меня уже лучше, чем у них, в трёх местах:**

- расхождение считается **при вводе**, а не после подтверждения;
- очередь **объяснена**: «деньги за них уже в ящике, это не излишек» —
  у донора просто число;
- **отклонённые названы отдельно** — их же правило из части 11,
  заведённое сюда.

**Их оговорка про связь мне не нужна.** У них сводка приходит с
сервера, а у меня «должно быть в ящике» считается **на кассе**:
внесённое минус сдача, с каждой оплаты. Закрытие работает без связи
всегда.

---

## Часть 12 · ShiftClose.tsx — закрытие смены ✔

**Что у них.**

*Расхождение при пересчёте:* «Сходится · Излишек · Недостача». — **есть
у меня.**

*Предупреждение об очереди:* «В очереди 3 неотправленных чека — они
уйдут вместе с закрытием».

*Закрытие без связи:* «Сводка недоступна без связи. Смену можно
закрыть — отчёт досчитается на сервере».

*Отчёт закрытия печатается* всегда.

**Сделано.**

**Кассир не знал про неотправленные чеки.** Он пересчитает деньги,
увидит **излишек** на сумму этих чеков и решит, что ошибся или что его
обвинят. Начнёт искать несуществующую ошибку, а очередь ждёт.

Теперь: «В очереди 3 неотправленных чека — они уйдут вместе с
закрытием. **Деньги за них уже в ящике, это не излишек.**»

**И про отклонённые — этого у них нет.** «Сервер не принял 2 чека —
деньги за них в ящике, а в отчёте их нет. Покажите владельцу.»

**Отчёт закрытия на бумаге.** Смена закрывалась молча: ни бумаги, ни
следа. А это **документ** — кассир сдаёт по нему деньги старшему. Без
бумаги сдают на словах, и любой спор — слово против слова.

```
             ЗАКРЫТИЕ СМЕНЫ
Отмен за смену                          2
Должно быть в ящике             184 200 ₸
Насчитано                       184 000 ₸
Не хватает                          200 ₸

Сдал: ______________
Принял: ____________
```

Не напечаталось — **смену всё равно закрываем**: бумага важна, но
держать кассира у открытой смены из-за ленты нельзя.

---

## Часть 13 · VoidFlow.tsx — отмена и код старшего ✔

**Что у них.** Весь путь одной машиной:

```
нужен PIN? ──да──▶ пад старшего ──▶ окно причины ──▶ готово
     └──────нет────────────────────▶ окно причины ──▶ готово
```

Их правило дословно:

> «Совсем незнакомый PIN без связи — отказ пропускается с **честной
> пометкой в отчёт**: гость уже отказался, зал не может ждать роутер.»

**Что уже есть у меня.** Все три пути их машины перенесены раньше:
код проверен сервером · код узнан по пропуску без связи · незнакомый
код без связи с пометкой «без связи, код старшего не проверен».

**Сделано.**

**Пометка не доходила до владельца.** Касса её ставила, а отправка
**отбрасывала**: в журнале владельца отмена выглядела обычной.

А это ровно то место, где касса уязвима: **интернет можно выключить
нарочно**. Кассир дёргает провод, отменяет позиции «без проверки» — и в
отчёте они неотличимы от разрешённых старшим.

Теперь отличимы. Не запрет — **след**.

Добавлены два поля: `offline_note` (как проверено) и `approved_name`
(имя словами — без связи ключа сотрудника может и не быть).

---

## Часть 13 · VoidFlow.tsx — код старшего без сети ✔

**Что у них.** Весь путь отказа одной машиной, и главное правило:

> «Пад старшего работает и без интернета: если старший уже входил на
> этой кассе, его PIN узнаваем локально, и права проверяются
> по-настоящему. Совсем незнакомый PIN без связи — отказ пропускается с
> честной пометкой в отчёт: гость уже отказался, **зал не может ждать
> роутер**.»

И их разделение ошибок: «Сервер жив и сказал „не подошёл" — это про
PIN, не про связь».

**Сделано.**

**Код старшего уходил только на сервер.** Сети нет — «PIN не подошёл»,
и **касса врёт**: код верный.

Старший вводит снова, ещё раз, злится. Отменить позицию нельзя вовсе —
покупатель ждёт, очередь стоит, товар в чеке лишний.

Теперь шесть случаев, каждый разобран:

| Случай | Итог | В отчёт |
|---|---|---|
| старший за кассой | разрешил молча | — |
| сервер жив, код верный | разрешил | — |
| сервер жив, код неверный | **отказ** — верим серверу | — |
| нет связи, код старшего | разрешил | «проверен на кассе (без связи)» |
| нет связи, код кассира | **отказ** — прав не даёт | — |
| нет связи, код чужой | пропущено | «без связи, код не проверен» |

**Пометка доходит до отчёта.** Поправлено 9 мест записи в журнал: без
неё владелец видит «отменил Ерлан» и не знает, что подпись проверена
**кассой**, а не сервером — а при незнакомом коде не проверена вовсе.

---

## Часть 14 · offline/queue.ts + api.ts — очередь ✔

**Что у них.**

> «Событие сначала ложится в журнал, потом уходит на сервер: связь
> оборвалась в момент оплаты — чек цел. Сервер **идемпотентен по
> eventId**, поэтому "отправить ещё раз" безопасно.»

**Сделано. Я СЛОМАЛ ОТПРАВКУ В ЧАСТИ 1 И НАШЁЛ ЭТО ЗДЕСЬ.**

В части 1 я перевёл ключи кассы на ULID — их запись, 26 знаков из букв
и цифр. Довод был верный: ключ растёт по времени, чеки лягут по
порядку.

Но поля ключей в моей базе типа **uuid**, и ULID туда не лезет:
`invalid input syntax for type uuid`.

**Ни один чек не ушёл бы на сервер.** Касса копила бы их в очереди, а
отправка падала с ошибкой 500 — и это заметил бы только владелец,
когда выручка не сойдётся.

Я проверил ULID **отдельно** — он работал. Но не проверил **весь путь
до сервера**. Ровно та ошибка, за которую ругал себя в части 10:
поменял запись — сверь все места, а не только то, где правил.

**Взял UUIDv7** — те же 36 знаков обычного вида, но первые двенадцать
это время. Сервер принимает, порядок держится.

```
попытка 1: accepted
попытка 2: duplicate
попытка 3: duplicate
```

Идемпотентность их правила у меня работает: повтор безопасен.

---

## Часть 14 · api.ts + queue.ts — обмен с сервером ✔

**Что у них.**

*Каталог из запаса при отказе:* «это несравнимо меньшая беда, чем
закрытая касса». — **есть у меня.**

*`catalogAge`:* когда меню в последний раз приходило с сервера.

*Связь и очередь — разные вещи:* «Раньше „онлайн" считали по пустой
очереди: одно застрявшее событие — и касса вечно показывала „Офлайн"
при живой сети, а кассир искал несуществующую поломку связи».

*Адрес сервера в ярлыке* — **у меня надёжнее**: хранится в настройках
оболочки и при переходах не теряется вовсе.

**Сделано.**

**Возраст каталога.** Касса без связи работает по сохранённому — верно.
Но владелец поднял цены вчера, а касса не выходила в сеть три дня: она
продаёт по **старым ценам**, и не знает об этом никто.

Теперь при трёх днях и больше: «Цены не обновлялись 3 дн. — если
владелец их менял, касса об этом не знает». Два дня терпимо, цены
меняют не каждый день.

**Связь проверяется отдельно от очереди.** У меня была **обратная**
беда, чем у них: пустая очередь давала **зелёную точку всегда**, даже
если сеть отвалилась час назад.

Кассир видит зелёное, пробивает чеки, а они копятся — и узнаёт об этом,
только когда первый попробует уйти.

Заведён короткий стук `/pos/ping`. Через сторожа устройства нарочно:
заодно узнаём, что устройство **не заблокировано** и магазин не
отключён — проверено, ловит.

---

## Часть 15 · WeightDialog + окна ввода ✔

**Что у них.**

*Вес руками или кнопкой «С весов»* через агента. — У меня **надёжнее**:
вес приходит **в штрихкоде весов** (префикс 22, граммы в коде), кассир
ничего не набирает вовсе.

*Предел количества:* `qty > 0 && qty <= 1000`.

*Своя клавиатура для сумм:* «системная либо не всплывает, либо
закрывает половину экрана». — **взято в части 7.**

**Сделано.**

**Марки не снимались при вводе цифрами.** В части 5 я починил кнопку
«−» и **не заметил, что цифры идут другим путём**.

```
было: 3 шт · марок 3
набрал «1» → 1 шт · марок 3   ← две лишние
```

Те же лишние марки в налоговую, та же беда — просто с другой кнопки.

**Урок записан в код:** чинишь правило про деньги — ищи **все** места,
где оно должно работать, а не то, откуда пришла жалоба.

Теперь одна свёртка `trimMarks` на все три пути: кнопка «−», ввод
цифрами, вес. Проверено — сходятся все три.

**Предела количества не было.** Опечатка в одну лишнюю цифру давала
**чек на миллион**: кассир набрал 99 вместо 9, промахнулся мимо кнопки
— и покупатель видит сумму с шестью нулями, а отменять её через
старшего.

Взял их предел: больше 1000 за раз — «похоже на опечатку».

---

## Часть 16 · их проверки на прочность ✔

**Что у них.** Пять файлов проверок, и в них записано, что ломалось.

*resilience.test.ts — три случая:* «Касса включается утром, а сеть у
заведения может лежать со вчера. Без запаса кассир увидел бы пустой
экран и не принял бы ни одного заказа — при том, что кухня работает и
гости пришли».

*queue.test.ts — пять случаев:* чек виден с настоящим итогом; связи нет
— ждёт, вернулась — ушёл; повтор безопасен, дубль снимается как
принятый; сервер ответил не про всё — неотвеченное ждёт; отклонённое не
исчезает молча.

**Сделано.**

**Написаны их проверки для моей кассы — 13 штук.** Не пересказ: гоняю
свой путь отправки и своё чтение каталога.

Все пять случаев очереди **прошли сразу** — путь верный:

- неотвеченное сервером ждёт следующей связи, а не теряется;
- дубль снимается как принятый (сервер отвечает `duplicate`, касса
  берёт всё, что не ошибка);
- отклонённое уходит в ящик, очередь пустеет.

**Третий случай каталога не проходил.** Ошибка глоталась молча: сети
нет и запаса нет — кассир видит **пустой каталог** и не понимает
почему. Ни товаров, ни объяснения. Думает, что касса сломалась, и
звонит.

Теперь: «Нет связи и нет сохранённых товаров — касса не сможет
продавать. Дождитесь интернета или позовите владельца».

**Молчим только когда запас есть** — тогда всё вправду в порядке:
торгуем по вчерашнему, и это меньшая беда, чем стоящая касса.

**Проверки подключены к выкладке:** не пройдут — заливка остановится.

---

## Часть 17 · electron/main.cjs — оболочка Windows ✔

**Что у них.**

*Сборка внутри программы, происхождение `file://` навсегда.* Их урок
дословно:

> «У страницы с сервера происхождение `https://…`, у страницы с диска —
> `file://`, а очередь чеков в IndexedDB у разных происхождений разная.
> **Переключение туда-сюда раздваивало бы деньги.**»

**Этой беды у меня нет по устройству:** очередь лежит в **файлах на
диске** через оболочку, а не в хранилище страницы. Откуда бы страница
ни грузилась, чеки в одном месте.

*Журнал рядом с настройками:* «Без него разбор „чек не вышел"
превращается в гадание: печатала ли программа, ответил ли принтер,
какой именно».

**Сделано.**

**Журнала оболочки не было вовсе.** Владелец звонит «чеки не
печатаются» — а магазин в другом городе. Что случилось: касса не
пыталась печатать, принтер молчал или печатала не в тот принтер?

**Без журнала не узнать** — приходится ехать и смотреть глазами.

Теперь в файле:

```
2026-08-20T17:10 печать: чек №57 · XP-80C · вышел
2026-08-20T17:10 печать: чек №58 · XP-80C · ОШИБКА: Принтер не найден
2026-08-20T17:10 печать: чек №58 · XP-80C · вышел
```

Три вопроса — три поля, больше не нужно. Пишем **только** про печать,
ящик и обновление: журнал, куда пишут всё, читать никто не станет.

**В меню «Журнал печати»** — рядом с настройками печати. Владелец
присылает файл вместо того, чтобы описывать беду словами.

**Не растёт без края:** полмегабайта, старое отрезается. Больше при
разборе всё равно не читают.

**Попутно найдено задвоение:** строка `receiptsRecent` в preload
оказалась записана дважды — от правки в части 10.

---

## Часть 18 · printer.cjs + обновление ✔

**Что у них.** Два правила печати, «выстраданных кассовой практикой»:

> **По очереди.** «Чек, бегунок кухни и бегунок бара могут прилететь в
> одну секунду. Если слать их параллельно, задания перемешиваются и
> **лента рвётся посреди чека**.»

> **Без общего доступа.** «Старый путь требовал расшарить принтер в
> Windows — лишний шаг настройки, о котором никто не знает и который
> слетает после обновлений.»

**Оба правила у меня уже есть** — перенесены раньше. Проверил очередь
на деле: три задания разом печатаются **строго по одному**.

**Обновление: у меня строже их.**

| | Они | Я |
|---|---|---|
| скачивание по кнопке | ✔ | ✔ |
| не ставить при выходе | ✔ | ✔ |
| проверка отпечатка | ✔ | ✔ |
| **при открытой смене не предлагать** | — | **✔** |

Их довод «установка безопасна в любой момент: очередь чеков живёт на
диске» верен **для чеков**. Но не для покупателя: касса уходит на
минуту, а он стоит с деньгами. Своё не трогаю.

**Откат к старой версии отбит** — у меня есть `isNewer`. Проверил
четыре случая, включая тот, где путаются: **2.10 новее 2.9**, если
сравнивать числами, а не строками. Строкой «2.10» меньше «2.9», и касса
откатилась бы на девять версий назад.

**Дописано 6 проверок** — печать по очереди и откат версии. Всего в
наборе кассы **19**.

---

